node_modules/
dist/
.env
.env.local

# Expo / React Native
.expo/
.expo-shared/
*.log

# Prisma
apps/backend/prisma/dev.db

# Projets natifs régénérés par `expo prebuild` à chaque build EAS.
# Le flux managé (CNG) ne les versionne pas : app.json est la source de vérité.
apps/mobile/android/
apps/mobile/ios/
